# Base URL of the PALS backend API.
# Local dev defaults to http://localhost:3000 when unset.
# In production (Vercel), set this to your deployed backend URL, e.g.
#   VITE_API_URL=https://pals-backend.example.com
VITE_API_URL=
